[build]
  command = "npm run build"
  publish = "dist"

# React Router — redirect all routes to index.html so client-side routing works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
